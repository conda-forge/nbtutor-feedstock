{% set name = "nbtutor" %}
{% set version = "2.0.1" %}
{% set sha256 = "ab3344ca7e2ae557417e477b5ff8f5a1f4f4af65949cb789e67c091aead4869a" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python

requirements:
  host:
    - python >=3.6
    - notebook
    - pip

  run:
    - python >=3.6
    - notebook

test:
  imports:
    - nbtutor

  requires:
    - pip

  commands:
    - pip check

about:
  home: https://github.com/lgpage/nbtutor
  summary: Visualize Python code execution in Jupyter Notebook cells
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENCE.md
  dev_url: https://github.com/lgpage/nbtutor

extra:
  recipe-maintainers:
    - lgpage
